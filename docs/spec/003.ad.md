已完整改造：**免费使用 + 广告变现 + 积分体系**，替代付费；
全程保持：**零知识、服务器无明文2FA密钥、E2EE加密**。

附上两套 Mermaid 图：
1. 业务全流程（广告→积分→解锁存储/同步额度）
2. 服务端架构（广告模块+积分系统+免费密文存储）

---

## 一、核心规则（产品逻辑）
1. 软件**永久免费**，无一次性付费、无会员订阅
2. 看激励广告 / 开屏广告 → 发放**安全积分**
3. 积分用途：
   - 解锁更多 2FA 密钥存储条数
   - 解锁跨设备云同步额度
   - 解锁备份导出、批量同步、长期云端留存
4. 无积分：限制少量本地2FA，**禁止云同步**
5. 安全底线不变：
   - TOTP密钥、安全密钥 全程不上传明文
   - 服务端只存加密密文，零知识

---

## 二、流程图：广告 + 积分 + 免费2FA同步
```mermaid
flowchart TD
    classDef client fill:#e6f7ff
    classDef server fill:#fef6e6
    classDef ad fill:#fef0f0
    classDef e2ee fill:#ffeef0

    A[用户免费下载App]:::client
    B{登录方式}
    B1[微信授权登录]
    B2[邮箱账号登录]
    B3[手机号验证码登录]
    
    C[服务端鉴权 分配UID]:::server
    D[默认权限<br/>仅本地5条2FA、无云同步]
    E[广告入口：开屏/激励视频]:::ad
    F[观看完整广告]
    G[服务端发放 安全积分]:::server
    H[积分兑换权益]
    H1[增加2FA存储上限]
    H2[解锁云端密文同步]
    H3[解锁多设备备份/恢复]

    I[拥有同步权限]
    J[客户端本地生成安全密钥]:::e2ee
    K[2FA数据本地AES加密]
    L[上传加密密文至服务器]:::server
    M[新设备登录+消耗积分]
    N[二选一获取安全密钥]
    N1[手动导入离线备份]
    N2[旧设备P2P扫码加密传输]
    O[本地解密 → 正常生成TOTP动态码]

    A --> B
    B --> B1 & B2 & B3 --> C --> D
    D --> E --> F --> G --> H
    H --> H1 & H2 & H3 --> I
    I --> J --> K --> L
    L --> M --> N --> N1 & N2 --> O
```

---

## 三、服务端架构图（广告+积分+零知识存储）
```mermaid
flowchart LR
    classDef user fill:#f0f8ff
    classDef api fill:#e8f4e8
    classDef admod fill:#ffebeb
    classDef store fill:#fff8e8
    classDef deny fill:#ffeeee

    U[用户客户端<br/>微信/邮箱/手机号登录]:::user

    subgraph 服务端整体
        A1[登录授权模块<br/>三方登录+短信验证]:::api
        A2[广告对接模块<br/>广告SDK回调、播放统计]:::admod
        A3[积分权益系统<br/>积分发放、抵扣、额度控制]:::api
        A4[密文同步服务<br/>增量上传/下载]:::api
    end

    subgraph 数据存储
        D1[用户基础库<br/>UID、绑定、设备列表]:::store
        D2[积分账单库<br/>广告收益兑换记录]:::store
        D3[加密云仓库<br/>仅存储AES密文Blob]:::store
    end

    %% 安全红线
    X[❌ 永不保存2FA明文]:::deny
    Y[❌ 永不保存安全密钥]:::deny

    U --> A1
    U --> A2
    A1 --> D1
    A2 --> A3 --> D2
    A3 --> A4 --> D3
    D3 --- X
    D1 --- Y
```

---

## 四、极简产品商业化方案（直接落地）
### 1. 广告形式（不打扰核心使用）
- 开屏广告：每次启动 3秒可跳过
- 激励广告：用户主动点击「扩容/开启同步」才弹出
- 无插屏暴力广告、不遮挡2FA动态码

### 2. 积分权益梯度
- 免费基础：本地 5 条 2FA，纯离线使用
- 1次激励广告：+10积分 → 解锁20条存储 + 7天云同步
- 3次广告：解锁永久云同步 + 多设备同步
- 每日看广告：领取每日积分，续期同步权益

### 3. 安全完全不受影响
- 广告、积分、账号系统 和 **加密内核完全隔离**
- 广告SDK无法读取本地2FA密钥、无法触碰加密数据
- 云同步依旧是：**本地加密→服务端存密文→本地解密**

---

需要我顺带给你：
- 极简「数据库表设计」（用户表、积分表、密文同步表）
还是
- 「前端UI逻辑文案」（广告弹窗、积分提示、权益说明）？