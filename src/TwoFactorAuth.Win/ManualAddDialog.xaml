<Window x:Class="TwoFactorAuth.Win.ManualAddDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="250" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="LblIssuer" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtIssuer" Grid.Row="1" Margin="0,0,0,10"/>
        <TextBlock x:Name="LblSecret" Grid.Row="2" Margin="0,0,0,4" TextWrapping="Wrap"/>
        <Grid Grid.Row="3" MinHeight="72">
            <TextBox x:Name="TxtSecret" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     Visibility="Collapsed"/>
            <PasswordBox x:Name="PwdSecret"/>
            <Button x:Name="BtnToggleSecret" Width="48" Height="24" Padding="4,0"
                    HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,4,4,0"
                    Click="BtnToggleSecret_OnClick"/>
        </Grid>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnOk" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Ok_OnClick"/>
            <Button x:Name="BtnCancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
