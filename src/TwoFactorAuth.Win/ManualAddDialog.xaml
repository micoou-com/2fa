<Window x:Class="TwoFactorAuth.Win.ManualAddDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="手动添加 TOTP" Height="250" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="显示名称（可选）" Margin="0,0,0,4"/>
        <TextBox x:Name="TxtIssuer" Grid.Row="1" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="2" Text="Base32 密钥（与网站 Secret / 扫码一致，可含空格或连字符）" Margin="0,0,0,4" TextWrapping="Wrap"/>
        <TextBox x:Name="TxtSecret" Grid.Row="3" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MinHeight="72"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="确定" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Ok_OnClick"/>
            <Button Content="取消" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
