<Window x:Class="TwoFactorAuth.Win.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="520" Width="720"
        WindowStartupLocation="CenterScreen">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="BtnManual" Width="100" Margin="0,0,8,0" Click="BtnManual_OnClick"/>
            <Button x:Name="BtnPaste" Width="120" Margin="0,0,8,0" Click="BtnPaste_OnClick"/>
            <Button x:Name="BtnDelete" Width="100" Click="BtnDelete_OnClick"/>
        </StackPanel>
        <ListView x:Name="AccountsView" SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn x:Name="ColAccount" Width="320" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn x:Name="ColCode" Width="140" DisplayMemberBinding="{Binding Code}"/>
                    <GridViewColumn x:Name="ColRemaining" Width="100" DisplayMemberBinding="{Binding Sec}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
